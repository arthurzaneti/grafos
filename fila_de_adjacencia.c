# include <stdio.h>
# include <stdlib.h>
# include "fila_de_adjacencia.h"
# include "fila.h"
# include "aresta.h"

FDA FDA_cria(int num_nos, fila nos, fila arestas){
    FDA *fda = malloc(sizeof(FDA));
    fda->vet = malloc(sizeof(fila)* num_nos);
    for(int i=0; i<num_nos; i++){
        
    }
}

void adiciona(tipo_no);
